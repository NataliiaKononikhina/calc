var button = document.querySelectorAll("button");
var input = document.querySelector(".culc__input");

button.addEventListener("click", function (evt) {
    document.getElementById('input').innerHTML = result;
});